---
description: Scaffold a new admin CRUD resource (types, client, composable, table, form, page, route, sidebar)
---

Add a new admin CRUD resource to this project.

Follow the skill at `.claude/skills/add-crud-resource/SKILL.md` step by step.

Details from the user: $ARGUMENTS

If any required information is missing (resource singular/plural, fields, whether
it has a slug, whether it needs a related dropdown), ask ONE consolidated question
before generating anything.

Before writing files, read the reference implementations listed in step 2 of the skill.

After generating files, print a short summary of what was created and what routes
are now available, so the user knows what to test.